lsd_generate.go -total=total_numbers_of_array -max=max_number_generated
the program lsd_generate.go creates an int array of len(array)="total". Then it gives each position a value between 0 and "max"
